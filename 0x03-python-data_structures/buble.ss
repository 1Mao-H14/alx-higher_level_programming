#!/usr/bin/python3
"""def max_integer(my_list=[]):
    if my_list == []:
        return None
    else:
        i = len(my_list) - 1
        trut = False
        while not trut:
            while i > 0:
                if my_list[i] > my_list[i-1]
                temp = my_list[i]
                my_list[i] = my_list[i-1]
                my_list[i-1] = temp
                i -= 1
                trut = False
        return my_list"""
